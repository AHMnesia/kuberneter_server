{{/*
AlertManager Ingress
*/}}
{{- if and .Values.alertmanager.enabled .Values.alertmanager.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "monitoring.fullname" . }}-alertmanager
  namespace: {{ .Values.namespace | default "monitoring" }}
  labels:
    {{- include "monitoring.labels" . | nindent 4 }}
    app.kubernetes.io/component: alertmanager
  {{- with .Values.alertmanager.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.alertmanager.ingress.className | default "nginx" }}
  {{- if .Values.alertmanager.ingress.tls }}
  tls:
  {{- range .Values.alertmanager.ingress.tls }}
  - hosts:
    {{- range .hosts }}
    - {{ . }}
    {{- end }}
    secretName: {{ .secretName }}
  {{- end }}
  {{- end }}
  rules:
  {{- range .Values.alertmanager.ingress.hosts }}
  - host: {{ .host }}
    http:
      paths:
      {{- range .paths }}
      - path: {{ .path }}
        pathType: {{ .pathType | default "Prefix" }}
        backend:
          service:
            name: {{ include "monitoring.fullname" $ }}-alertmanager
            port:
              number: {{ .servicePort | default 9093 }}
      {{- end }}
  {{- end }}
{{- end }}