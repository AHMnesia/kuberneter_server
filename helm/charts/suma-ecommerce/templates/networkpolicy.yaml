{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "suma-ecommerce.fullname" . }}-ingress
  namespace: {{ .Values.namespace | default "suma-ecommerce" }}
  labels:
    {{- include "suma-ecommerce.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "suma-ecommerce.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchExpressions:
              - key: name
                operator: In
                values:
                  {{- toYaml .Values.networkPolicy.allowedNamespaces | nindent 18 }}
{{- end }}
