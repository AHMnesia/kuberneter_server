apiVersion: v1
kind: Service
metadata:
  name: {{ include "suma-ecommerce.fullname" . }}
  labels:
    {{- include "suma-ecommerce.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "suma-ecommerce.selectorLabels" . | nindent 4 }}