apiVersion: v1
kind: Service
metadata:
  name: {{ include "suma-office.fullname" . }}
  labels:
    {{- include "suma-office.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  ports:
    - name: http
      port: {{ .Values.service.port | default 8000 }}
      targetPort: {{ .Values.service.port | default 8000 }}
      protocol: TCP
  selector:
    {{- include "suma-office.selectorLabels" . | nindent 4 }}
