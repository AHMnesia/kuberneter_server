apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/component: dashboard
    app.kubernetes.io/instance: default
    app.kubernetes.io/part-of: tekton-dashboard
  name: tekton-dashboard
  namespace: tekton-pipelines

---
# (Dashboard non-CRD resources follow)
# The CustomResourceDefinition `extensions.dashboard.tekton.dev` was removed from
# this file and stored in `tekton/tekton-crds-backup.yaml`.
