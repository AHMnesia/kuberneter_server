apiVersion: v1
kind: Service
metadata:
  name: suma-android-api-service
  namespace: suma-android
  labels:
    app: suma-android-api
    environment: production
spec:
  type: ClusterIP  # Internal service untuk production
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: suma-android-api
    environment: production
