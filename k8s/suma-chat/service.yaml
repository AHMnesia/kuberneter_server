apiVersion: v1
kind: Service
metadata:
  name: suma-chat
  namespace: suma-chat
  labels:
    app: suma-chat
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 4000
      protocol: TCP
  selector:
    app: suma-chat
