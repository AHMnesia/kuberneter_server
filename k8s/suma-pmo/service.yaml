apiVersion: v1
kind: Service
metadata:
  name: suma-pmo-api-service
  namespace: suma-pmo
  labels:
    app: suma-pmo-api
    environment: production
spec:
  type: ClusterIP  # Internal service untuk production
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: suma-pmo-api
    environment: production
